# Contributing to the Straw Go SDK

Keep the SDK independent of `straw-oss/internal/...`, add focused tests for public behavior, and run `make check`.
Protocol changes must first land as an exact `straw-protos-go` tag. Report vulnerabilities through
[SECURITY.md](SECURITY.md), not an issue.
